<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dkm.partymachine"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="8" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:name=".DKMPartyMachineActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DrinksActivity" android:label="Drinks">
            <intent-filter><action android:name=".DrinksListView"/>
            </intent-filter>
        </activity>
        <activity android:name=".EventsActivity" android:label="Events">
            
        </activity>
        <activity android:name=".PicturesActivity" android:label="Pictures">
            
        </activity>
    </application>

</manifest>